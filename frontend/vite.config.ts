import { defineConfig } from 'vite';

export default defineConfig({
  // Set base path for GitHub Pages deployment.
  // Change 'PickerWheelApp' to match your repo name.
  base: '/PickerWheelApp/',
});
